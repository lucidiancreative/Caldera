// Placeholder: the React island mounts but renders nothing yet. The Schedule view
// (SchedulePage) is built and verified in isolation; it now has full feature parity
// and gets routed in here next.
export function App() {
  return null;
}
